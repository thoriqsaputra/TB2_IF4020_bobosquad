{
  "name": "bobo-cert-smart-contract",
  "version": "1.0.0",
  "description": "Smart Contract for Digital Certificate System - Person 1",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy-smart.js --network sepolia",
    "deploy:sepolia": "hardhat run scripts/deploy-smart.js --network sepolia",
    "deploy:localhost": "hardhat run scripts/deploy-smart.js --network localhost",
    "deploy:force": "set FORCE_DEPLOY=true&& hardhat run scripts/deploy-smart.js --network sepolia",
    "deploy:check": "node -e \"console.log(JSON.parse(require('fs').readFileSync('deployment.json','utf8')))\"",
    "test": "hardhat test",
    "coverage": "hardhat coverage",
    "node:local": "hardhat node",
    "clean": "hardhat clean"
  },
  "keywords": [
    "solidity",
    "ethereum",
    "smart-contracts",
    "certificates",
    "blockchain"
  ],
  "author": "Person 1",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "^17.2.3"
  }
}
